# ✅ locator.py 생성용 프롬프트
LOCATOR_PROMPT = f"""
당신은 selenium + pytest 기반 QA 자동화 엔지니어입니다.
다음 우선순위 규칙에 따라 **모든 요소의 로케이터를 하나씩 추출해주세요**

우선순위 규칙:
1순위: ID
2순위: CSS Selector
3순위: XPATH (ID, CSS가 불가능할 때만 사용) 

출력 형식:
아래 마크다운 형식으로 각 페이지별 로케이터를 정리해서 출력해주세요.
문자 사이에 공백을 넣지 말고 일반적인 마크다운 텍스트로 출력하세요.

```markdown
# 웹 요소 로케이터 목록

## [페이지명]

| 요소 | 타입 | 로케이터 |
|------|------|----------|
| 로그인 버튼 | CSS | .btn.btn-primary |
| 이메일 필드 | ID | email-input |
| ... | ... | ... |
```

아래는 HTML입니다:
"""